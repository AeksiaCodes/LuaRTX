local vector = require("vector")
local ray = require("ray")

ori = vector.new(1, 2, 3)
dir = vector.new(1, 0, 0)

myRay = ray.new(ori, dir)
print(v:heading())
  
  rayFromPoint(0, 0, 0, z.x, z.y, z.z, 50, 0.1)